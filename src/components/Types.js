/* eslint-disable */
import React from 'react';

const Types = ({ selectItem }) => (
  <div>
    <button onClick={() => selectItem('combination')}>combination</button>
  </div>
);

export default Types;
